services:
  translation-api:
    image: ghcr.io/YOUR_GITHUB_USERNAME/everybuddy-ai:latest
    ports:
      - "55186:8000"
    env_file:
      - .env
    restart: unless-stopped
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
